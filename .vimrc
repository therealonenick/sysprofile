set number
set tabstop=4
set cursorline
syntax on
" Uncomment the following line to set colorscheme, otherwise will use default.
" For a list of color schemes, run 'ls -l /usr/share/vim/vim*/colors/'
" colorscheme darkblue
